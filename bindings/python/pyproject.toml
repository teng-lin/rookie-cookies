[build-system]
requires = ["maturin>=1.2,<2.0"]
build-backend = "maturin"

[project]
name = "rookie-cookies"
description = "Cross-platform browser cookie extraction and decryption for Python"
readme = "README.md"
license = "MIT"
keywords = [
    "browser cookies",
    "cookie extraction",
    "cookie decryption",
    "Chrome",
    "Firefox",
    "Safari",
    "App-Bound Encryption",
]
authors = [
    {name = "thewh1teagle"},
    {name = "Teng Lin"},
]
# Version comes from bindings/python/Cargo.toml via maturin. Newer maturin
# (1.6+) requires an explicit declaration here; the older defaults are gone.
dynamic = ["version"]
requires-python = ">=3.11"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Rust",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Internet :: WWW/HTTP :: Browsers",
    "Topic :: Internet :: WWW/HTTP :: Session",
]

[project.urls]
Homepage = "https://github.com/teng-lin/rookie-cookies"
Documentation = "https://github.com/teng-lin/rookie-cookies/blob/main/bindings/python/README.md"
Repository = "https://github.com/teng-lin/rookie-cookies"
Issues = "https://github.com/teng-lin/rookie-cookies/issues"
Changelog = "https://github.com/teng-lin/rookie-cookies/blob/main/CHANGELOG.md"
Security = "https://github.com/teng-lin/rookie-cookies/security/policy"

[tool.maturin]
features = ["pyo3/extension-module"]
